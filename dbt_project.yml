name: 'ecom_dbt_project'
version: '1.0.0'
config-version: 2

profile: 'default' # Bağlantı profili

model-paths: ["models"]
test-paths: ["tests"]

# Katmanlı Mimari Yapısı (Şirketlerin en çok baktığı yer)
models:
  ecom_dbt_project:
    staging:
      +materialized: view # Ham veriler hızlı erişim için View yapılır
    marts:
      +materialized: table # Raporlama tabloları performans için Table yapılır
